'use strict';

/**
 * @ngdoc function
 * @name gainmasterWebClientApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the gainmasterWebClientApp
 */
angular.module('gainmasterWebClientApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
