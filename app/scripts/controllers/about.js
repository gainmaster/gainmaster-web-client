'use strict';

/**
 * @ngdoc function
 * @name gainmasterWebClientApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the gainmasterWebClientApp
 */
angular.module('gainmasterWebClientApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
